# Luna · Max 설치가이드 웹 검증

- 대상: `/guides/luna-max`
- 날짜: 2026-08-12
- 상태: 브랜치 검토본, 배포 전

## 자동 검사

| 항목 | 데스크톱 1440px | 모바일 390px |
| --- | --- | --- |
| HTTP 응답 | 200 | 200 |
| 문서 폭 / 화면 폭 | 1440 / 1440 | 390 / 390 |
| 가로 넘침 | 없음 | 없음 |
| `main` 요소 | 1개 | 1개 |
| 복사 버튼 | 5개 | 5개 |
| 심각·치명 접근성 오류 | 0 | 0 |
| 콘솔 오류 | 0 | 0 |

## 빌드

- `npm run lint`: 오류 0, 기존 스크립트 경고 11건
- `npm run typecheck`: 통과
- `next build --webpack`: 통과, `/guides/luna-max` 정적 생성 확인
- Turbopack: 작업트리 밖 `node_modules` junction을 거부해 환경 오류 발생. `docs/patterns/PAT-008-turbopack-worktree-junction.md`에 별도 기록

## 시각 검토

- 제목과 본문을 좌측 기준선에 맞추고, 단계 숫자와 연결선을 독립 레일로 분리했습니다.
- 모바일 단계 마커는 46px, 복사 버튼은 최소 높이 44px로 보정했습니다.
- 명령어는 줄바꿈으로 변형하지 않고 가로 스크롤 가능한 원문으로 유지했습니다.
- 카드뉴스의 보라색 궤도 언어는 히어로에만 사용하고, 설치 본문에서는 대비와 단계 순서를 우선했습니다.

## 교차검토

- 1차 판정: FAIL — Luna 전용 흐름에 Native 어댑터 설치가 섞였고, 요청문이 별도 advisor review를 요구했습니다.
- 수정: Luna 전용은 설정값 저장까지만 안내하고, Native 설치를 선택 단계로 분리했습니다. 최종 diff 검토와 승인은 메인 작업 책임으로 교정했습니다.
- 재판정: PASS — 높음 이상 결함 없음.

## 산출물

- `desktop.png`: 데스크톱 전체 페이지 캡처
- `mobile.png`: 390px 모바일 전체 페이지 캡처
