# Luna · Max 설치 가이드 디자인 QA

## 작업 컨텍스트

- 목표: 승인된 인스타그램 카드뉴스의 다크·바이올렛 시각 언어를 Yohan Studio 전용 설치 가이드로 확장한다.
- 대상 경로: `/guides/luna-max`
- 상태: 데스크톱·모바일 반응형, 복사 인터랙션, 가독성, 실제 브랜드 이미지 사용을 검증했다.

## 비교 입력

| 구분 | 파일 | 픽셀 | 상태 |
| --- | --- | --- | --- |
| 승인 원본 | `docs/content/reviews/codex-luna-max-guide-v2/web-preview/approved-source.png` | 1440 × 6833 | 승인된 기준 |
| 최종 구현 | `docs/content/reviews/codex-luna-max-guide-v2/web-preview/final-current.jpg` | 1730 × 8869 | 최종 CSS 반영 |
| 모바일 상단 | `docs/content/reviews/codex-luna-max-guide-v2/web-preview/final-360-top.png` | 360 × 786 | 제목·이미지 확인 |
| 동일 화면 비교 | `docs/content/reviews/codex-luna-max-guide-v2/web-preview/comparison-side-by-side.jpg` | 1730 × 4906 | 원본·구현 병렬 비교 |

## 시각 검증

- 승인 원본의 검정 배경, 보라색 포인트, 굵은 제목, 세로 단계선, 명령 카드, 문제 해결 그리드를 유지했다.
- CSS로 임의 생성한 궤도 이미지는 제거하고 승인 자산 `docs/content/exports/codex-luna-max-guide/assets/source/max-rings.png`를 사용했다.
- 설치 정확성을 위해 Windows v0.5.0 경고, 6개 환경 표, 읽기 전용 스모크 테스트 단계를 추가했다. 정보량 증가는 의도된 차이이며 디자인 구조는 유지했다.
- 본문 색 대비는 배경 조합별 최소 8.58:1, 기본 잉크는 최소 16.75:1로 WCAG AA를 충족한다.
- 보조 라벨과 모바일 명령문 글자 크기를 최소 14px로 올렸다.

## 반응형 계측

| 뷰포트 | 가로 넘침 | 본문 폭 | 주요 결과 |
| --- | --- | --- | --- |
| 360px | 없음 | 360px | 제목의 `를` 단독 줄바꿈 없음, 이미지 로드 |
| 432px | 없음 | 432px | 환경 표가 카드형으로 전환 |
| 768px | 없음 | 768px | 단계와 명령 카드 정상 |
| 1280px | 없음 | 1280px | 데스크톱 2열 구조 정상 |
| 1440px | 없음 | 1440px | 원본과 동일한 정보 위계 유지 |

## 인터랙션·접근성

- 첫 번째 명령 복사 버튼은 `명령 복사`에서 `복사됨 ✓`로 상태가 변경됐다.
- 모든 명령 블록은 키보드 포커스를 받을 수 있고, 링크·복사 버튼에는 명확한 `focus-visible` 윤곽선이 있다.
- 환경 표 컨테이너에 키보드 포커스와 접근성 이름을 제공했다.
- 제목·설명·단계 제목은 의미론적 제목 구조를 사용하며, 장식 요소는 보조기기에서 제외했다.

## 발견 및 수정 이력

| 우선순위 | 발견 | 수정 | 결과 |
| --- | --- | --- | --- |
| P2 | 360px에서 조사 `를`이 한 줄로 고립됨 | `Luna · Max를`을 함께 줄바꿈하는 강조 구간으로 묶음 | 해결 |
| P2 | 전용 페이지와 공통 푸터 사이에 밝은 여백이 생김 | 전용 페이지의 `main` flex와 배경을 경로 한정으로 보정 | 해결 |
| P2 | 초기 궤도 그래픽이 승인 원본이 아닌 CSS 근사치였음 | 승인된 실제 PNG 자산으로 교체 | 해결 |
| P2 | 코드·표·보조 라벨 일부가 12~13px로 작음 | 최소 14px로 상향 | 해결 |

미해결 P0/P1/P2 없음.

final result: passed
